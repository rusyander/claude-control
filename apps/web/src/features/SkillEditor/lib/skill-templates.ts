/**
 * Библиотека заготовок тела SKILL.md.
 *
 * Начинать скилл с пустого поля тяжело: непонятно, какие разделы нужны. Здесь —
 * набор готовых форм тела (frontmatter name/description собирается из полей формы
 * отдельно). Выбор заготовки заполняет поле «Инструкции» рабочим каркасом, а
 * текст самих заготовок локализован — лежит в i18n под `skills.templates.<id>`,
 * поэтому здесь только перечень идентификаторов, без дублирования содержимого.
 *
 * Заготовки — данные, а не файлы-зависимости: чтобы добавить новую форму,
 * достаточно дописать id сюда и три строки (title/hint/body) в оба словаря i18n.
 */

/** Идентификаторы встроенных заготовок тела SKILL.md, в порядке показа. */
export const SKILL_BODY_TEMPLATE_IDS = ['blank', 'tool', 'rule', 'checklist'] as const;

export type SkillBodyTemplateId = (typeof SKILL_BODY_TEMPLATE_IDS)[number];
