/**
 * Потолки и списки исключений обзора проекта.
 *
 * Каталог здесь чужой — настоящий рабочий проект, а не раздел панели, — поэтому
 * все ограничения заданы явно: без них один `node_modules` превращает дерево в
 * сотню тысяч записей, а один дамп на 300 МБ — в попытку сравнить его построчно.
 */

/**
 * Каталоги, которые в дерево не попадают. Это не «скрыть лишнее», а
 * работоспособность: содержимое здесь генерируется, и править его в панели
 * незачем. `.git` в списке отдельно — заглядывать в служебный каталог
 * репозитория из редактора кода нельзя тем более.
 *
 * `.gitignore` намеренно не разбирается: правил в нём бывает больше, чем
 * файлов в проекте, а частичная их поддержка врала бы о том, что показано.
 */
export const IGNORED_DIRS = new Set([
  '.git',
  '.hg',
  '.svn',
  'node_modules',
  '.pnpm-store',
  'dist',
  'build',
  'out',
  'coverage',
  '.next',
  '.nuxt',
  '.turbo',
  '.cache',
  '.venv',
  'venv',
  '__pycache__',
  '.gradle',
  '.idea',
]);

/** Записей в одном каталоге. Больше — отдаём срез и говорим об этом клиенту. */
export const MAX_DIR_ENTRIES = 1000;

/**
 * Больше этого файл не открывается в редакторе: панель — не просмотрщик дампов,
 * а браузер такой текст всё равно не удержит.
 */
export const MAX_FILE_BYTES = 2 * 1024 * 1024;

/**
 * Потолок отдельно для картинок и PDF: их не читают в память построчно и не
 * держат в JSON — браузер тянет байты сам, поэтому предел здесь щедрее
 * текстового. Скриншот на 5 МБ в проекте обычное дело, и отказывать в показе
 * из-за текстового ограничения было бы враньём о причине.
 */
export const MAX_MEDIA_BYTES = 25 * 1024 * 1024;

/**
 * Порог построчного сравнения. Выше него дифф не строится вовсе: LCS квадратичен
 * по числу строк — ровно та же граница и по той же причине, что в предпросмотре
 * записи (`domains/provider-preview.ts`).
 */
export const MAX_DIFF_CHARS = 400_000;

/**
 * Сколько изменённых файлов отдаём списком. Разговор, тронувший больше, читают
 * не по дереву, а по git; полотно на тысячу строк тут никому не помогает.
 */
export const MAX_CHANGED_FILES = 300;
