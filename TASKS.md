# Очередь работ

Что осталось сделать в панели. Разделено по тому, чем задача закрывается: **кодом** — можно взять и
сделать прямо сейчас; **живой проверкой** — код написан и покрыт тестами, но никто не видел его на
настоящей машине, и написать тут нечего, пока не посмотришь. Внутри каждой части — по серьёзности:
что происходит с пользователем, если не делать. Завершённое отсюда убирается: его история — в
коммитах.

## Решается кодом

Партии от 04.09.2026 «аудит чата» (Т1–Т12), от 06.09.2026 «паритет телефона и качество» (Т1–Т6) и от
06.09.2026 «эталон» (Т1–Т5) закрыты 06.09.2026; разбор каждой задачи — в
`.agent/archive/TASKS-done-2026-09-06.md` (локальный файл вне git).

## Партия от 07.09.2026 «подбор модели под задачу» — Т1–Т13 сданы, открыт только туман

Не «подмена модели», а конвейер: панель понимает, ЧТО за работа в группе, ставит под неё модель и
обязательно проверяет результат более сильной моделью. Смысл — не экономия денег (их и не тратится,
подписка), а расход окна лимитов: веер из пяти детей на потолке съедает его быстрее всего, а
освободившееся окно окупается вторым проходом по той же работе. Качество при этом должно РАСТИ:
дешёвая модель делает, дорогая проверяет.

### Алгоритм целиком

**1. Классифицирует агент, назначает панель.** Модель плохо выбирает модель («мне хватит sonnet» —
это оценка себя, а не задачи), но неплохо называет РОД работы. Поэтому в блоке разделения агент
пишет каждой группе `kind` из закрытого набора, а модель и глубину выводит таблица в коде:

| `kind`           | что это                                                       | назначение    |
| ---------------- | ------------------------------------------------------------- | ------------- |
| `mechanical`     | переименования, одна и та же правка во многих файлах, формат  | sonnet/medium |
| `implementation` | понятная правка или фича с готовыми критериями приёмки        | sonnet/high   |
| `tests`          | тесты по готовой спецификации                                 | sonnet/high   |
| `investigation`  | причина неизвестна: плавающий баг, производительность, разбор | потолок       |
| `design`         | архитектура, контракты, миграции, безопасность, необратимое   | потолок       |
| `review`         | проверка чужой работы                                         | потолок       |
| нет / незнакомый | —                                                             | потолок       |

Таблица живёт в коде (`contracts/model-cascade.ts`), а не в промпте: её видно, её можно поправить
одной строкой, и она одинакова для всех прогонов. `haiku` в таблице НЕТ вовсе — автоматически он не
назначается никогда, только руками на карточке.

**2. Агент может просить БОЛЬШЕ, но не меньше.** Поля `model`/`effort` в предложении остаются: если
агент считает группу сложнее её класса, он поднимает — и панель это принимает (до потолка). Просьба
ПОНИЗИТЬ ниже класса игнорируется. Так модель не может сэкономить на себе, но может настоять на
своей осторожности; «сомневаешься → потолок» получает механику, а не обещание.

**3. Панель поднимает по фактам, а не по словам.** До клэмпа — правила на данных, которые у панели
есть без модели: группа из пяти и более задач либо задание длиннее 4000 знаков — не механика, ранг
+1; потолок ниже opus — не понижаем вовсе (экономить нечего, риск остаётся).

**4. Понижение оплачивается проверкой.** Ребёнок, поехавший НИЖЕ потолка, получает в задании планку:
перед сдачей прогнать проверки проекта и сверить результат с критериями; а после его успешного
прогона панель сама заводит следующее звено — **ревью на потолке** по его же диффу в его же копии, и
при найденных замечаниях третье звено — **фикс на модели работы**. Ребёнок, ехавший НА потолке,
ревью не получает: усиливать нечем.

_Этап 2 (Т10) сдан и принят живьём 07.09.2026._ Звенья ревью и правок работают: после
успешной работы понижённого ребёнка панель сама заводит ревью на потолке в той же копии, а по его
замечаниям — правки обратно на модели работы. Тексты правила, справки и заданий теперь говорят об
этом прямо: скрывать проверку от агента незачем — знающий о ней оставляет незаконченное
незаконченным.

**5. Звенья — это продолжения одного чата, а не новые агенты.** Параллельных чатов ровно столько,
сколько групп (потолок 8); «работа → ревью → фикс» идут ПОСЛЕДОВАТЕЛЬНО в той же ветке и той же
копии механикой handoff (`HANDOFF_MAX_CHAIN` = 5 уже есть). Следующее звено стартует само, без
вопроса, и только после УСПЕШНОГО предыдущего — предохранители те же, что у продолжения. Карточка
разделения показывает счёт ДО запуска: «5 чатов · до 13 прогонов».

**6. Выключатель — правило в правах чата, область проекта.** Тумблер «Подбирать модель под задачу»
стоит рядом с правилами прав в меню чата, по умолчанию ВКЛЮЧЁН, и запоминается на проект: выключен в
этом проекте — все дети едут на модели, выбранной человеком, конвейера нет, поведение прежнее.

**Инварианты (держит код, не промпт):** никогда выше потолка; `max` не назначается никому; назначать
можно только алиасы CLI (`haiku`/`sonnet`/`opus`/`fable`) — устаревшее поколение недостижимо по
построению, алиас разворачивается в свежую модель семейства; нераспознанный потолок → конвейера нет.

Этапы: 1 — подбор модели в разделении (Т1–Т9); 2 — конвейер звеньев ревью/фикс (Т10–Т11); 3 — чужие
провайдеры (Т12). Область покрыта тестами: `chat-routes.split.integration.test.ts`, юниты контрактов,
стабовый `tools/qa/check-task-split.mjs` — новые ветки ложатся в готовую сеть.

### Этап 1 сдан 07.09.2026 (Т1–Т9)

Подбор модели работает в разделении целиком: контракт и клэмп, классы в предложении, правило проекта в меню чата, строка агенту, назначение детей и планка сдачи, память модели после первого прогона, карточка с классом и ручной заменой, наследование модели продолжением, справка. Разбор каждой задачи — `.agent/archive/TASKS-done-2026-09-07.md` (локальный файл вне git).

Одно отличие от спецификации: ручная замена с карточки едет ОТДЕЛЬНЫМ полем запроса (`assignments`), а не внутри предложения агента: его просьба действует только вверх, выбор человека — в обе стороны, и внутри одного объекта их не различить.

### Т10 сдан и принят живьём 07.09.2026 — конвейер «работа → ревью → фикс»

Конвейер звеньев работает целиком: успешный прогон понижённого ребёнка заводит ревью на записанном
потолке в той же копии и ветке, непустой список замечаний — правки обратно на модели работы, а
группа с пустым диффом ревью не получает вовсе. Разбор задачи и живой приёмки —
`.agent/archive/TASKS-done-2026-09-07.md` (локальный файл вне git).

### Т11 сдан и проверен живьём 07.09.2026 — сводка звеньев у родителя

В родительском разговоре под ответом агента висит сводка групп: строка на группу — ветка, пройденный
путь («работа › ревью › правки»), модель нынешнего звена и пульсирующая точка, если оно идёт; клик
открывает звено в этом же окне. Группа держится веткой, а без неё — именем из связи: заголовок чата
это текст его первого сообщения, и у работы, ревью и правок он разный. В пульте агентов у каждого
прогона теперь написано, чем он ведётся, — включая подхваченные после F5 и заведённые не этой
вкладкой. Справка дополнена карточкой «Где смотреть, кто на каком звене».

Попутно исправлен дефект, найденный живым прогоном: алиас CLI разворачивался в РЕКОМЕНДОВАННУЮ
модель уровня, а не в свежую (`sonnet` → `claude-sonnet-4-6` при доступном Sonnet 5), то есть
понижение ранга молча уводило группу на прошлое поколение вопреки обещанию партии. Теперь алиас в
конкретное имя разворачивает сама панель, по каталогу моделей; справка исправлена там же.

### Т12 сдан и проверен живьём 07.09.2026 — подбор модели у чужих провайдеров

Разделение задач у чужого CLI больше не раздаёт всем группам одно и то же: класс решает ровно то
же, что у Claude, но понижать умеют только провайдеры, у которых способ передать модель
**документирован** — codex (`-m` плюс `model_reasoning_effort`, единственный аналог глубины во всём
наборе) и gemini (`-m`). Остальные живут без подбора, и причина записана в карточке самого
провайдера: у qwen в семействе мешаются flash/plus/max, opencode — шлюз к чужим вендорам, у kimi
`-m` называет строку собственной таблицы моделей, у aider/goose/continue списка моделей нет вовсе,
у cursor нет чата. Потолка у чужого CLI не существует (потолок — то, что настроено в самом CLI,
панель умеет только понижать), поэтому классы уровня потолка не получают ни одного флага: прогон
идёт ровно так, как без панели. Разбор задачи и живой проверки —
`.agent/archive/TASKS-done-2026-09-07.md` (локальный файл вне git).

### Конвейер ревью у чужих провайдеров — сдан и проверен живьём 08.09.2026

Отложенное в Т12 сделано, и не тем способом, которого мы боялись: чужие прогоны никуда не
протаскиваются. Конвейер живёт своим модулем (`domains/provider-chat/cascade.ts`) на завершении
ответа и стадии в шапке разговора — реестр прогонов Claude при этом не трогается вовсе. Понижённая
группа у чужого CLI теперь получает ревью: работа кончилась и в копии есть дифф → панель заводит
разговор «‹группа› · ревью» **без подобранной ступени** (у чужого CLI потолка нет, его настройка и
есть единственное «выше»), ревьюер отвечает машинным блоком, и по непустому списку замечаний
заводится «‹группа› · правки» обратно на модели работы. Цепочка конечна по построению: ревью
заводится один раз, после правок не бывает ничего.

Проверено живьём (`.agent/tmp/live-foreign-cascade.mjs`): настоящий маршрут разделения, настоящие
копии веток через `git worktree`, настоящий запуск процесса; подставлен только сам CLI, которого на
машине нет. Заодно нашлась и закрыта дыра показа: блок вердикта — служебный, у Claude его убирает
лента, а у чужого чата своя, и она показывала человеку сырой JSON.

### Т13 сдан и проверен живьём 07.09.2026 — журнал понижённых прогонов и полировка веера

Понижение больше не бесследно: каждый такой прогон дозаписывается в `<appData>/lowered-runs.json`
(потолок 200 записей, отдельный файл — испорченный журнал не должен стоить человеку его групп), а
аналитика показывает карточкой, чем вели, дошли ли до проверок и не упал ли прогон. Планка сдачи
именно **наблюдается**: `ChatRunRegistry` ловит запуски проверок в событиях `Bash`, но панель их не
запускает — это были бы лишние прогоны окна лимитов. У веера счёт прогонов равен числу выбранных
проектов, с третьего агента на потолке под кнопкой появляется цена, а на пустом реестре кнопка
рисуется выключенной с объяснением, а не исчезает. Площадь закрыта двумя проверками с подменой API:
`tools/qa/check-{parallel-model,lowered-runs}.mjs`. Серверная половина уехала внутри чужого коммита
`c35e1fd`, веб-половина — своим `9a12f26`. Разбор и живые проверки —
`.agent/archive/TASKS-done-2026-09-07.md` (локальный файл вне git).

Попутно: `docs/CHAT-GUIDE.ru.pdf` — руководство по чату для человека, со снимками, собирается из
`.agent/chat-guide/index.html` (`tools/docs/{shots-chat-guide,build-chat-guide}.mjs`). Исходник и
кадры лежат в рабочей папке агента, вне git: в репозитории только готовый PDF, а снимки снимаются с
полностью подменённым API, поэтому пересобираются на любой машине. И `audit-layout` перестал
вечно числить дефектом непереносимый текст `TruncatedText` — описания плагинов и навыков извне
панели.

### Ещё не специфицировано (туман, не тикеты)

Пусто: последний пункт закрыт 08.09.2026 — **расход по классам работы показывается человеку**.
Карточка понижённых прогонов раскладывает их по классам (сколько прогонов, сколько окна съел класс,
сколько раз проверок не было видно); прогоны ручного веера класса не имеют и собираются строкой «без
класса». Классификатору эти числа не сообщаются **никогда** и сообщаться не будут: узнав цену
классов, он начнёт метить механикой всё подряд. Правит таблицу «класс → модель» человек, и решение
это его.

Остальное из этого списка сделано 07.09.2026 (коммит `810842c`):

- **Ступень `fable`** — живой `claude --model fable -p ok` ответил, ступень на аккаунте есть;
  добавлена в выбор модели (`shared/lib/chat-model`), иначе потолок на неё ставился только
  конкретным id из каталога.
- **Ручной веер сам выбирает ступень** — окно параллельного запуска считает лестницу от потолка
  разговора (`manualAssignment`), подписывает понижение и шлёт `model`/`effort` + `lowered`;
  сервер разворачивает алиас и добавляет планку сдачи — **без автоматического ревью**: конвейер
  звеньев живёт на копиях веток, а веер работает в настоящих каталогах проектов. Заодно починен
  тумблер правок: окно снова открывается разрешёнными, как и написано в его же комментарии.
- **Телефон показывает, чем ведут прогон** — `AgentRun.model` берётся из события `session` (телефон
  модель не шлёт, а ребёнок разделения едет на выбранной панелью), значок рядом с расходом.
  Проверено на живом устройстве 08.09.2026: релизный APK на эмуляторе, спаренный с панелью через
  `adb reverse`, задача отправлена **с телефона** — в шапке `opus-4-8`, снимки
  `.agent/screenshots/mobile/`. Значок принадлежит прогону, а не разговору: после конца хода он
  пропадает, поэтому снимать его можно только на живом прогоне.

### Порядок и проверка партии

Гейт этапа 1 пройден 07.09.2026: `type-check`, `lint`, `test` (2995 серверных + 794 веб),
`depcruise`, живой стенд `audit-layout`, `check-a11y`, `check-keyboard`, `check-etag`,
`check-task-split`, `check-help` и мобильный type-check — всё зелёное.

**Живая приёмка этапа 1 пройдена 07.09.2026** на одноразовом каталоге `C:\work\cascade-probe` при
потолке `claude-opus-5`/`high`, без единой подмены API. Подтвердилось всё: агент сам расставил классы
(`mechanical`/`tests`/`design`), в карточке видны классы, модели и счёт прогонов, дети стартовали
ровно на назначенном (доказано командными строками живых процессов), второе сообщение ребёнку модель
не потеряло, правило помнится на проект и переживает перезагрузку, а выключенным возвращает прежнее
поведение. Разбор и снимки — `.agent/screenshots/live-acceptance/model-cascade/`.

**Живая приёмка конвейера звеньев (Т10) пройдена 07.09.2026** на одноразовом репозитории `C:\work\cascade-stage2`
при том же потолке. Разделение на две группы завело копии веток; обе поехали на `sonnet`/`high`
(понижение при потолке `claude-opus-5`). После успешной работы панель сама завела ревью на потолке в
той же копии — командная строка живого процесса показала `--model claude-opus-5 --effort high`.
Ревьюер ничего не правил, нашёл настоящий дефект (`Number('') === 0`, из-за чего `parseRange('-5')`
возвращал диапазон вместо ошибки) и закончил блоком вердикта; по нему панель завела правки обратно
на `sonnet`/`high`, и они закрыли замечание — `npm test` в копии 8/8. Группа, которая ничего не
меняла, ревью не получила: пустой дифф проверять не зовут. Вся цепочка «работа → ревью → фикс»
уложилась в четыре минуты. Снимки — `.agent/screenshots/live-acceptance/model-cascade-stage2/`.

Приёмка этапа 1 вскрыла и починила отдельный дефект: дети разделения не наследовали автоподтверждение и
вставали на первом же вызове `Bash`. Тумблер взводится под временным ключом `new-…`, карточка
присылает `sessionId`, а сводил их реестр только пока завершённый прогон лежит в буфере (минута) —
человек читает карточку дольше. Починено в `ChatRunRegistry` (память о выбывших ключах) и
`ChatSession` (чистка карты больше не выбрасывает родителя), закрыто двумя регрессионными тестами.

## Партия от 07.09.2026 «внешние интеграции + список нехватки Test IT» — сдана

Atlassian (облако и Server/DC), фордж по токену, Telegram, Zephyr/Xray, junit из CI, привязка
проекта и групп к задачам и страницам, собственный MCP-переходник для агента; шесть дыр из отчёта по
тестовому контуру — матрица покрытия, статус дефекта из трекера, устойчивый `externalId`, карантин,
вехи и релизы, общий вебхук; переходник MCP у остальных девяти CLI. Что именно сделано и чем
проверено — `.agent/archive/TASKS-done-2026-09-07.md`.

Открытым остаётся только то, что нельзя проверить без ваших ключей, — см. следующий раздел.

## Партия от 08.09.2026 «руководство по тестам и управляемая генерация» — спецификация, код позже

Партия спроектирована 08.09, не написано ни строки: сначала решаем, что строим. Две задачи ваши —
руководство в PDF и генерация кейсов/планов/групп; остальное добавлено после разбора того, что в
тестовом контуре уже есть и чего ему не хватает до эталона.

### Ваши две задачи — оценка честная

**Руководство в PDF — последним, решено 08.09 вами.** Первоначально оно стояло первым (написать по
сегодняшней подсистеме, в конце перевыпустить). Ваше решение по ходу работы: не писать дважды —
подсистема в этой же партии получает шесть новых экранов, и проза о них будет переписана целиком, а
не подправлена. Поэтому Т1 сливается с Т15 и делается ОДИН раз, после сдачи Т2–Т14. Приёмка UX
инструкцией при этом не теряется — она просто случается позже.

Уже сделано под Т15 (задел, не переделывать): общий сборщик печати `tools/docs/build-guide.mjs`
(руководство по чату переведено на него, PDF выходит байт в байт прежним) и съёмка
`tools/docs/shots-tests-guide.mjs` со сплошь подменённым API — 12 кадров из 20 снимаются, дальше
скрипт упирается в окно ручного прохода. Кадры всё равно пересниматься будут: доводить съёмку до
конца имеет смысл в Т15, а не сейчас.

**Генерация — формулировка неточная в одном.** Режим `generate` уже есть и работает
(`domains/project-tests/prompt.ts:182`): агент изучает проект и пишет кейсы прямо в файлы групп.
Строить надо не генерацию, а УПРАВЛЯЕМОСТЬ ею. Сегодня прогон пишет в настоящую библиотеку без
спроса, дубликаты не ловит ничто, а увидеть сделанное можно только диффом git — и на пятой генерации
библиотека превращается в свалку почти одинаковых кейсов, которую никто не чистит. Отсюда Т2–Т4. И
отдельно: планы и таксономию генерировать агентом НЕ надо — это счётная работа (Т5). Правила дешевле
токенов, воспроизводимы и закрываются юнит-тестом, а модель тут добавляет только разброс.

### Что нашлось при разборе — важнее обеих ваших задач

**Два режима прогона написаны целиком и не имеют ни одной кнопки.** `explore` — исследовательская
сессия по турам (плохой ввод, прерывание, границы данных, права и состояния, повтор) с записью
найденного новыми кейсами; `automate` — превращение стабильных кейсов в автотесты с запуском и
проставлением `automation`. Оба в `MODES` (`routes/project-tests/run-routes.ts:16`), у обоих готовы
задания (`prompt.ts:283` и `:322`) и границы прав. Ни панель, ни телефон, ни CLI их не запускают.
Самая дешёвая крупная прибавка партии — Т6.

Дальше — то, чего нет вовсе и без чего контур остаётся складом кейсов, а не инструментом решения:
сравнение прогонов, доказательства провала, самоочистка карантина, риск и время, отчёт готовности
релиза. Это Т7–Т12.

### Порядок, цена и что урезать

Этапы: **B** — управляемая генерация (Т2–Т5); **C** — прогон как доказательство (Т6–Т9); **D** —
решения по релизу (Т10–Т12); **E** — эргономика и паритет (Т13, Т14, Т16); **F** — руководство (Т15,
вобравшее в себя Т1). Внутри этапа задачи независимы, между этапами — только Т3 после Т2, Т16 после
Т12 (секрет окружения вводится в его же окне) и Т15 после всего.

Если урезать до минимума, дающего большую часть пользы: **Т2, Т6, Т7, Т10, Т15**. Управляемая
генерация, две готовые кнопки, сравнение прогонов, риск-отбор и руководство — этого хватает, чтобы
контуром пользовались каждый день. Остальное — качество, а не наличие.

Не в объёме всей партии: телефон (кроме того, что и так придёт с API), новые внешние интеграции,
любые правки чат-ветки (её ведёт соседняя сессия), собственный формат автотестов.

**Решено 08.09:** берём все этапы; генерация приезжает черновиком с необязательной галочкой
автоприёмки (Т2); проверка набора намеренной поломкой остаётся в тумане до конца партии.
**Уточнено 08.09 по ходу работы:** руководство переезжает в конец партии (Т1 → Т15), чтобы не писать
прозу дважды.

### Т1. Руководство по тестовому контуру в PDF — слито с Т15

Отложено 08.09 вашим решением: подсистема в этой партии меняется слишком сильно, чтобы описывать её
дважды. Требования задачи целиком перенесены в Т15 — там же и критерии приёмки. Сделанный задел
(общий сборщик, съёмка, 12 кадров) описан выше в «Ваши две задачи».

### Т2. Генерация пишет черновик; приёмка — руками или по галочке

- **Источник:** ваши слова «генерация кейсов» + развилка, решена 08.09 · **Где:** сервер + «Библиотека»
- **Симптом:** прогон `generate` правит настоящие файлы групп без спроса. Что он сделал — видно
  только `git diff`; в проекте без git не видно никак. Отменить нечем.
- **Код:** `domains/project-tests/prompt.ts` (`generatePrompt`, `schemaBlock`), `run-permissions.ts`
  (сейчас `generate` пишет во весь `.agent/tests/`), `store.ts` (слияние по `id`),
  `routes/project-tests/run-routes.ts`, вкладка `pages/Tests/TestsLibraryTab.tsx`.
- **Решение по форме:** агент пишет `.agent/tests/drafts/<runId>.draft.json` —
  `{version:1,runId,source,createdAt,items:[{op:'add'|'update',groupId,caseId,case,before?,reason,similarTo?}]}`;
  `before` — снимок прежней версии, без него откат невозможен. Библиотеку меняет ТОЛЬКО панель,
  применяя черновик: прогон не имеет прав на файлы групп ни при какой галочке.
- **Галочка «принимать сразу, без просмотра»:** по умолчанию выключена и необязательна. Стоит в двух
  местах — в форме запуска и в самом окне приёмки, чтобы можно было посмотреть первые предложения
  глазами и включить приём для остальных. Принятое автоматически помечается (`source:'agent'`,
  `readiness:'draft'`), видно фильтром и откатывается одной кнопкой.
- **Ожидаемо (критерии приёмки):**
  - [ ] прогон `generate` не меняет ни одного файла группы **ни при какой галочке** — доказано
        тестом прав, а не обещанием
  - [ ] галочка по умолчанию выключена, помнится на проект, и её срабатывание видно строкой в записи
        прогона: «принято автоматически, N кейсов»
  - [ ] включение галочки в окне приёмки применяет оставшиеся предложения этого прогона и следующие
  - [ ] «Отменить приёмку» убирает добавленные кейсы и возвращает изменённые из `before`; кейс,
        который человек успел поправить или прогнать, не трогается и называется в отчёте отката
  - [ ] `op:'delete'` в черновике нет вовсе: удаление кейсов остаётся человеку
  - [ ] черновик переживает перезапуск панели; отклонённый уезжает в `drafts/archive/`; вторая
        генерация не затирает непринятый черновик; битый черновик гасит только себя
  - [ ] приёмка при занятой прогоном группе — 409 с именем прогона, как у остальных записей
- **Проверка:** юниты домена и прав; стабовый сквозняк `tools/qa/check-tests-generate.mjs`; живой
  прогон на одноразовом проекте — при выключенной галочке библиотека до и после байт в байт, при
  включённой кейсы появляются с пометкой и откатываются одной кнопкой.
- **Не в объёме:** приёмка черновика с телефона, правка кейса прямо в окне приёмки.
- **Статус:** ⏳ спецификация

### Т3. Источники генерации: требование, дифф, дефект

- **Источник:** развитие вашей задачи, 08.09 · **Где:** `prompt.ts`, run bar, вкладки покрытия и прогонов
- **Зачем:** генерация «по коду» отвечает на «что тут есть», но не на вопросы, которые задают в
  работе: покрыть требование, проверить то, что изменилось в ветке, закрепить починенный баг.
- **Код:** `ProjectTestRunRequest.source` в контрактах; блоки задания в `prompt.ts`; кнопки в
  `features/ProjectTests/ui/ProjectTestsRunBar.tsx`, «покрыть требование» в
  `pages/Tests/TestsCoverageTab.tsx`, «завести регрессионный кейс» у провала в `TestsRunsTab.tsx`;
  текст задачи берётся существующими интеграциями (`domains/integrations`).
- **Ожидаемо (критерии приёмки):**
  - [ ] `requirement`: в кейсы автоматически кладётся `links[{type:'requirement',url}]`, и строка в
        матрице покрытия перестаёт быть «непокрытой» сразу после приёмки черновика
  - [ ] `diff`: база и вершина по умолчанию `origin/main..HEAD`, в задание уходят пути и сводка (не
        сами хунки), `codePaths` проставляются из диффа автоматически
  - [ ] `defect`: шаги, `note` и вложения провала переносятся в регрессионный кейс со ссылкой на
        задачу; исходный кейс не трогается
  - [ ] интеграция выключена → источник `requirement` недоступен с внятной строкой, а не 500
- **Проверка:** юниты на сборку задания под каждый источник; стабовый сквозняк; живьём — по одному
  кейсу на источник на одноразовом проекте.
- **Не в объёме:** генерация из транскрипта разговора, из логов продакшена.
- **Статус:** ⏳ спецификация

### Т4. Дубликаты и линтер библиотеки

- **Источник:** разбор, 08.09 · **Где:** `domains/project-tests/`, отчёт, фильтры библиотеки
- **Зачем:** без этого генерация — машина по производству мусора: на третьем прогоне в наборе три
  «Вход с пустым паролем», и ни один человек их не сведёт руками.
- **Код:** новые `similar.ts` (нормализованные множества токенов заголовка и шагов, мера Жаккара,
  порог подбирается на настоящей библиотеке панели) и `lint.ts`; карточка «Здоровье набора» в
  `pages/Tests/TestsReportTab.tsx`, фильтр «с замечаниями» в `features/ProjectTests/ui/TestFilterBar.tsx`.
- **Правила линтера:** нет `oracle`; у кейса шаг без `expected`; нет `codePaths`; нет приоритета;
  больше 15 шагов; `%параметр` в шагах не объявлен и наоборот; повтор заголовка внутри группы;
  `obsolete` не в архиве; `draft` старше 30 дней; кейс не гонялся N дней; чек-лист с ожиданием.
- **Ожидаемо (критерии приёмки):**
  - [ ] похожий кейс виден в приёмке черновика строкой «похоже на `gui-014` (82%)» с переходом
  - [ ] каждое замечание линтера — идентификатор, серьёзность, кейс и предлагаемое массовое действие
  - [ ] линтер ничего не правит сам; правка — только существующим bulk по кнопке человека
  - [ ] на библиотеке этой панели линтер отрабатывает меньше секунды и не даёт ложных дублей
- **Проверка:** юниты (пары «дубль/не дубль» из настоящей библиотеки), `pnpm tests lint` (Т14).
- **Не в объёме:** автослияние дублей, эмбеддинги и любые внешние модели ради похожести.
- **Статус:** ⏳ спецификация

### Т5. Планы и таксономия собираются правилами, а не агентом

- **Источник:** ваша задача «генерация планов/групп», решение о форме — моё · **Где:** вкладка «Планы»
- **Зачем:** план — это отбор по счётным признакам (приоритет, риск, время, веха, дифф). Агент здесь
  добавляет только разброс и расход окна; правило считается мгновенно и проверяется тестом.
- **Код:** `domains/project-tests/plans.ts` (+ новые генераторы), `impact.ts`, `runs-store.ts`
  (нестабильные), `coverage.ts` (требования вехи); UI — `features/TestPlans/`; таксономия — новый
  `taxonomy.ts` поверх существующего bulk `section`/`move`.
- **Ожидаемо (критерии приёмки):**
  - [ ] четыре генератора плана: «дым за N минут» (набор под бюджет `duration` по приоритету и
        риску), «регрессия по диффу» (impact + соседи по `area`), «план вехи» (кейсы требований
        релиза + всё красное с прошлой вехи), «нестабильные» (stability ниже порога)
  - [ ] предпросмотр до сохранения: сколько кейсов, сумма минут, что НЕ влезло и почему
  - [ ] план сохраняется обычным `ProjectTestPlan`, дальше правится руками как любой другой
  - [ ] таксономия предлагает только переносы (`section`/`move`), содержимое кейсов не меняет
        никогда, применяется одной кнопкой после предпросмотра «перенести N кейсов»
  - [ ] ноль токенов: генераторы работают без запуска агента, в том числе на офлайн-машине
- **Проверка:** юниты на каждый генератор; живьём — сборка плана на библиотеке этой панели.
- **Не в объёме:** автоматическое перестроение таксономии по расписанию.
- **Статус:** ⏳ спецификация

### Т6. Кнопки для `explore` и `automate`

- **Источник:** находка разбора, 08.09 · **Где:** run bar панели, вкладка «Прогоны», CLI
- **Симптом:** два полноценных режима есть на сервере и недостижимы для человека.
- **Код:** `features/ProjectTests/ui/ProjectTestsRunBar.tsx` (сейчас только `generate` и три вида
  `run`), `prompt.ts:283` (`explorePrompt`), `:322` (`automatePrompt`), `run-permissions.ts`
  (у `automate` уже разрешены файлы автотестов, названные кейсами).
- **Ожидаемо (критерии приёмки):**
  - [ ] «Исследовать» требует хартию (`scope`) — без неё кнопка не активна: сессия без хартии
        превращается в блуждание
  - [ ] находки исследования приезжают кейсами со статусом провала и шагами воспроизведения
  - [ ] «Автоматизировать» доступен только когда в отборе есть кейсы, не помеченные `automated`, и
        показывает, сколько их
  - [ ] после `automate` в кейсах проставлен `automation.file`/`testName`, и импорт из CI сводит
        результаты по `externalId`/`testName` без «нераспознанных»
  - [ ] оба режима видны в истории прогонов и в отчёте наравне с остальными
- **Проверка:** стабовый `check-tests-runner.mjs` дополняется двумя режимами; живьём — исследование и
  автоматизация одного кейса на одноразовом проекте с настоящим агентом.
- **Не в объёме:** запуск этих режимов с телефона.
- **Статус:** ⏳ спецификация

### Т7. Сравнение прогонов и перепрогон провалов

- **Источник:** разбор, 08.09 · **Где:** вкладки «Прогоны» и «Отчёт»
- **Зачем:** главный вопрос после прогона — «что изменилось с прошлого раза», и ответа на него нет
  нигде: человек сравнивает два списка глазами.
- **Код:** `domains/project-tests/runs-store.ts` (записи прогонов уже лежат целиком),
  `pages/Tests/TestsRunsTab.tsx`, `TestsReportTab.tsx`; список «перепроверить» уже считает
  `defect-status.ts`, но им ничего не запускается.
- **Ожидаемо (критерии приёмки):**
  - [ ] `diffRuns(a,b)`: новые провалы · починенные · всё ещё красные · не тронутые · новые кейсы
  - [ ] у записи прогона — «сравнить с предыдущим», в отчёте — блок «с прошлого прогона»
  - [ ] «Перепрогнать провалившиеся» стартует прогон ровно по этим `caseIds`
  - [ ] «Перепроверить» запускает прогон по списку из статусов дефектов (провал при закрытой задаче)
  - [ ] сравнение прогонов разных планов и окружений честно говорит, что наборы разные
- **Проверка:** юниты на пары записей (включая «кейс исчез», «кейс появился»); стабовый сквозняк.
- **Не в объёме:** графики трендов сверх существующего `TestsTrend`.
- **Статус:** ⏳ спецификация

### Т8. Провал без доказательства — не результат

- **Источник:** разбор, 08.09 · **Где:** задания прогона, запись результата
- **Зачем:** контракт давно умеет пошаговые результаты и вложения (`ProjectTestPointResult.steps`,
  `attachments`), но агентские прогоны их не заполняют: «провалился» без указания шага и без снимка
  нельзя ни воспроизвести, ни завести дефектом.
- **Код:** `prompt.ts` (`runPrompt`), `domains/project-tests/store.ts` (`applyResults`),
  `attachments.ts`, карточка провала в `TestsRunsTab.tsx`.
- **Ожидаемо (критерии приёмки):**
  - [ ] в задании прогона прямо требуется: номер провалившегося шага, что ожидалось, что вышло,
        доказательство во вложении
  - [ ] провал без доказательства помечается в отчёте («нет доказательства»), но результат
        принимается: панель не выбрасывает работу агента из-за формальности
  - [ ] провалившийся кейс проходится второй раз в том же прогоне: совпало — провал подтверждён,
        разошлось — помечается нестабильным, обе попытки в `note`
  - [ ] черновик дефекта берёт номер шага и вложение автоматически
- **Проверка:** юниты на разбор результатов; живой прогон с намеренно сломанным экраном.
- **Не в объёме:** видеозапись прогона, трассировка Playwright.
- **Статус:** ⏳ спецификация

### Т9. Карантин с самоочисткой и устаревание кейсов

- **Источник:** разбор, 08.09 · **Где:** отчёт, библиотека
- **Зачем:** карантин без срока годности — это тихое удаление кейса. А набор, который никто не
  пересматривает, за полгода расходится с приложением и начинает врать.
- **Код:** `store.ts` (`muted`/`muteReason`), `runs-store.ts` (`flakyCases`), `coverage.ts`,
  `defect-status.ts`; интеграция Atlassian даёт дату правки задачи.
- **Ожидаемо (критерии приёмки):**
  - [ ] N зелёных подряд у кейса в карантине → предложение снять карантин (снимает человек)
  - [ ] стабильность ниже порога → предложение поставить карантин, причина обязательна
  - [ ] «не гонялся дольше N дней» и «требование изменилось в трекере позже кейса» — отдельные
        строки в здоровье набора, с переходом на кейс
  - [ ] ни одно из предложений не применяется само
- **Проверка:** юниты на истории прогонов; стабовый сквозняк на карточке здоровья.
- **Не в объёме:** автоматическое снятие карантина.
- **Статус:** ⏳ спецификация

### Т10. Риск и время: что гонять, если есть полчаса

- **Источник:** разбор, 08.09 · **Где:** библиотека, планы, run bar
- **Зачем:** отбор «всё подряд» не помещается ни в один рабочий день, а отбор «по приоритету»
  игнорирует историю: кейс, который падал вчера, важнее ровно того же кейса, зелёного год.
- **Код:** новый `domains/project-tests/risk.ts`; данные уже есть — приоритет, история прогонов,
  нестабильность, давность, попадание в `impact`.
- **Ожидаемо (критерии приёмки):**
  - [ ] риск считается из пяти множителей, формула описана в комментарии и закрыта юнитами
  - [ ] сортировка «по риску» в библиотеке и в составе плана
  - [ ] «У меня N минут» набирает кейсы под бюджет по `duration` и честно пишет, что не влезло
  - [ ] кейс без истории не получает нулевой риск: неизвестность — тоже риск
- **Проверка:** юниты на порядок; сверка глазами на настоящей библиотеке панели.
- **Не в объёме:** обучение чего-либо на истории, веса из аналитики.
- **Статус:** ⏳ спецификация

### Т11. Отчёт готовности релиза одним документом

- **Источник:** разбор, 08.09 · **Где:** отчёт, PDF, публикация
- **Зачем:** вопрос «отдаём или нет» задают вехой, а ответ сейчас собирается из четырёх вкладок.
- **Код:** `domains/project-tests/export-run.ts` и `pdf.ts` (печать уже есть, Chromium со своим
  профилем), `coverage.ts` (требования), `runs-store.ts` (`releases`, `untested`), публикация в
  Confluence уже кнопкой (`TestsRunPublish.tsx`).
- **Ожидаемо (критерии приёмки):**
  - [ ] один документ: веха → требования → кейсы → прогоны → дефекты → непроверенное → строка-вердикт
  - [ ] непроверенное и открытые дефекты вынесены наверх: это и есть ответ
  - [ ] печатается тем же путём, что отчёт прогона; нет браузера → честный 501 с именем того, что
        поставить
  - [ ] ни одной пустой страницы (правило вёрстки PDF), проверено постранично
- **Проверка:** живая печать на проекте с двумя вехами; постраничный просмотр.
- **Не в объёме:** почтовая рассылка отчёта, расписание.
- **Статус:** ⏳ спецификация

### Т12. Секреты окружения для прогона

- **Источник:** разбор, 08.09 · **Где:** окружения, запуск прогона
- **Зачем:** прогон против настоящего стенда упирается в логин, а класть пароль в `environments.json`
  (файл в git проекта) нельзя. Сегодня это тупик: тестируется только то, что открыто без входа.
- **Код:** `environments.json` хранит только ССЫЛКУ на ключ; сам секрет — в существующем шифрованном
  хранилище (`lib/provider-keys.ts`, ключ вида `env:<projectPath>:<envId>`); подстановка в прогон —
  `domains/project-tests/runs.ts` переменными окружения процесса.
- **Ожидаемо (критерии приёмки):**
  - [ ] секрет не попадает ни в задание агенту, ни в лог прогона, ни в запись, ни в ответ API
  - [ ] в UI показывается маской, вводится и стирается человеком
  - [ ] в файле проекта — только имя переменной; удаление окружения удаляет и секрет
  - [ ] секрета нет → прогон стартует, но пишет в лог, какой переменной не хватило
- **Проверка:** юниты на отсутствие секрета во всех исходящих строках; живой прогон против стенда с входом.
- **Не в объёме:** общий менеджер секретов проекта, секреты в CI.
- **Статус:** ⏳ спецификация

### Т13. Эргономика ручного прогона

- **Источник:** разбор, 08.09 · **Где:** `features/TestRunner/`, пустое состояние библиотеки
- **Симптом:** в ручном прогоне нет ни одной горячей клавиши: сто кейсов — это триста попаданий мышью
  по трём кнопкам. Пустой проект встречает пустым списком без единой подсказки, с чего начать.
- **Код:** `features/TestRunner/ui/TestRunnerModal.tsx`, `model/useManualRunner.ts`;
  `pages/Tests/TestsLibraryTab.tsx` (пустое состояние).
- **Ожидаемо (критерии приёмки):**
  - [ ] клавиши: прошёл / провал / блок, следующий, заметка, снимок; подсказка видна на экране
  - [ ] клавиши не перехватываются, когда курсор в поле ввода
  - [ ] пустой проект показывает три шага: окружение → генерация → первый прогон
  - [ ] проходит существующие проверки доступности и клавиатуры без новых замечаний
- **Проверка:** `check-a11y`, `check-keyboard`, стабовый сквозняк ручного прогона; before/after кадры.
- **Не в объёме:** настраиваемые раскладки клавиш.
- **Статус:** ⏳ спецификация

### Т14. Паритет CLI и CI

- **Источник:** разбор, 08.09 · **Где:** `tools/tests-cli.mjs`, `.github/workflows/ci.yml`
- **Зачем:** всё новое, что нельзя вызвать без панели, в CI не существует.
- **Код:** `tools/tests-cli.mjs` (сейчас `list|show|run|import|export|report`).
- **Ожидаемо (критерии приёмки):**
  - [ ] `lint` — замечания набора, код выхода 1 при заданной серьёзности
  - [ ] `diff <runA> <runB>` — сравнение прогонов текстом и в junit
  - [ ] `plan <вид>` — сборка плана правилами, без панели и без агента
  - [ ] `help` перечисляет всё новое; тексты русские, как в остальном CLI
- **Проверка:** прогон каждой команды на одноразовом проекте, проверка кодов выхода.
- **Не в объёме:** публикация отчёта из CLI во внешние системы.
- **Статус:** ⏳ спецификация

### Т16. Окружения, общие шаги и свои поля: экранов нет вовсе

- **Источник:** найдено 08.09 при работе над руководством — первый же раздел «заведите окружение»
  писать оказалось не про что · **Где:** раздел «Тестирование»
- **Симптом:** `environments.json`, `_shared.steps.json` и `schema.json` панель ЧИТАЕТ и всюду
  использует (окружение в пульте и в плане, общий шаг в редакторе кейса, своё поле колонкой в
  таблице), но завести их можно только руками в файле. Клиентские методы для этого есть и не вызваны
  ни из одного компонента: `ProjectTestApi.ts:144` (общий шаг), `:163` (окружение), `:182` (схема) —
  маршруты сервера тоже готовы. То есть дыра ровно в одном слое: экрана нет.
- **Код:** `apps/web/src/entities/ProjectTest/api/ProjectTestApi.ts` (методы уже есть),
  `features/ProjectTests/` (новое окно настроек набора), `pages/Tests/TestsPage.tsx` (кнопка рядом с
  выбором проекта), сервер не трогается.
- **Ожидаемо (критерии приёмки):**
  - [ ] одно окно «Настройки набора» с тремя разделами: окружения, общие шаги, свои поля
  - [ ] окружение заводится, правится, помечается «по умолчанию» и убирается в архив; удаление
        окружения, на которое ссылается план, честно предупреждает, а не рвёт план молча
  - [ ] общий шаг заводится и правится там же, где его вставляют в кейс, и показывает, в скольких
        кейсах он используется
  - [ ] своё поле (`text`/`select`/`number`, обязательность, варианты) появляется колонкой в таблице
        и полем в редакторе сразу после сохранения
  - [ ] битый файл по-прежнему гасит только себя: окно показывает причину и не перезаписывает файл
  - [ ] секрет окружения (Т12) вводится в этом же окне маской — двух мест для одного окружения нет
- **Проверка:** юниты на слияние файлов; стабовый сквозняк `check-project-tests.mjs` дополняется
  разделом; живьём — завести окружение и своё поле на одноразовом проекте.
- **Не в объёме:** импорт окружений из чужой TMS, шаблоны наборов.
- **Статус:** ⏳ спецификация

### Т15. Руководство по тестовому контуру в PDF (вобрало Т1)

- **Источник:** ваши слова, 08.09 · **Где:** `tools/docs/`, `.agent/tests-guide/`, `docs/`
- **Зачем:** контуром нельзя пользоваться, не зная его; справка в панели отвечает на «что это за
  кнопка», а на «как за день пройти путь от пустого проекта до отчёта» не отвечает никто.
- **Код:** общий сборщик `tools/docs/build-guide.mjs` уже вынесен (чат переведён на него,
  `build-tests-guide.mjs` рядом), съёмка `tools/docs/shots-tests-guide.mjs` доведена до 12 кадров из 20. Исходник `.agent/tests-guide/index.html` и кадры — вне git, готовый PDF
  `docs/TESTS-GUIDE.ru.pdf` — в git.
- **Ожидаемо (критерии приёмки):**
  - [ ] `docs/TESTS-GUIDE.ru.pdf` в git; исходник и кадры — в `.agent/tests-guide/`, вне git
  - [ ] кадры снимаются с полностью подменённым API: ни одного личного проекта и пути с машины
  - [ ] маршрут дня целиком: пустой проект → соглашение в `CLAUDE.md` → окружение → генерация и
        приёмка черновика → библиотека (секции, фильтры, параметры, общие шаги) → план → прогон
        агентом → исследование → ручной прогон → дефект → сравнение прогонов → отчёт и тренд →
        покрытие требований → карантин и веха → риск и время → отчёт релиза → импорт из CI → PDF
        прогона → `pnpm tests` в CI
  - [ ] описаны все экраны, появившиеся в партии: приёмка черновика, источники генерации,
        исследование и автоматизация, сравнение прогонов, риск и время, отчёт релиза, секреты
        окружения, горячие клавиши ручного прохода
  - [ ] в каждом разделе — «зачем», а не только «куда нажать»; на каждый экран свой кадр
  - [ ] ни одной пустой и полупустой страницы: `PAGES=` разложить постранично и просмотреть глазами
  - [ ] пересборка на свежем клоне — две команды, без ручной правки путей
  - [ ] ни одного утверждения, не сверенного с живой панелью
- **Проверка:** `node tools/docs/shots-tests-guide.mjs` при поднятом стенде →
  `node tools/docs/build-tests-guide.mjs` → постраничный просмотр глазами.
- **Не в объёме:** английская версия, видео, перенос содержимого в `docs/*.md`.
- **Статус:** ⏳ спецификация, задел сделан

### Ещё не специфицировано (туман, не тикеты)

- **Проверка самого набора поломкой.** Единственный честный ответ на «а поймают ли эти кейсы
  регрессию» — внести заведомую поломку и убедиться, что нужный кейс покраснел. Механика в панели уже
  есть: параллельная копия репозитория (`worktrees`), права прогона, автотесты у части кейсов. Дорого
  и по времени, и по окну лимитов, поэтому берётся только по отдельному вашему слову.
- **Кейсы из живого разговора.** Транскрипт чата содержит сценарий, который человек только что
  прошёл руками; превращать его в кейс — заманчиво, но чат ведёт соседняя сессия, и трогать его
  сейчас нельзя.
- **Свои типы проверок** (доступность, скорость) отдельным видом кейса — пока не ясно, чем такой
  кейс отличается от обычного, кроме оракула.

## Не решается кодом — нужна живая машина

Здесь нечего писать, пока не запустишь: код есть, тесты зелёные, но подмена — не запуск.

- **Телефон после партии 06.09.** Холодный старт после конца прогона, сторож потока (30 с тишины →
  переподключение), очередь и переподхват после перезапуска закрыты 12 юнит-тестами стора, но на
  устройстве не запускались: собрать APK, повторить сценарии Т1–Т2 из
  `.agent/archive/TASKS-done-2026-09-06.md`.
- **Живая приёмка интеграций — нужны ваши ключи.** Код собран и покрыт тестами с подменой, но
  настоящего Atlassian, настоящего Telegram и настоящего приёмника вебхука панель на этой машине не
  видела. Ключи вводите вы сами в панели (агенту они не передаются): сайт и API-токен Atlassian →
  проверка связи, одна настоящая задача Jira по провалу, одна публикация в Confluence, одно
  сообщение в Telegram, один POST на свой адрес с проверкой подписи, один вызов MCP из настоящего
  прогона агента.
- **Красный статус CI на GitHub.** `.github/workflows/ci.yml` написан, его шаги локально зелёные; что
  намеренно сломанный PR красится в красный, видно только после пуша.
  06.09 прогон не стартовал дважды: «account is locked due to a billing issue» — снять блокировку в
  GitHub, затем `gh run rerun 34027816828` или любой пуш.
- **Сторож `keepalive` не установлен.** Перезапуск dev-сервера 05.09 22:30:54 закрыт как
  невоспроизводимый (`node --watch` перезапускается от одного касания mtime файла из графа импортов;
  правок агентов в ту минуту не было). Следующий такой случай оставит след только после
  `pnpm keepalive:install` — решение владельца.
- **Первая реальная запись у чужих CLI.** Aider, Cursor, OpenCode, Continue, Goose, Kimi Code и Qwen
  Code на машине разработки не установлены: их форматы собраны по документации и закрыты
  round-trip-тестами, но глазами первую запись никто не видел. Проверять по одному: поставить CLI,
  открыть раздел, записать, сверить файл вручную.
- **Первый живой разговор у чужого CLI.** Чат собран на задокументированном неинтерактивном запуске
  и покрыт тестами на форме argv и на потоке, но настоящего ответа настоящего CLI никто не видел.
  Проверять по одному: поставить CLI, задать вопрос, убедиться, что текст идёт по мере печати, а
  второй вопрос помнит первый. У OpenCode отдельно — что сессия `opencode serve` поднимается, а её
  заминка роняет разговор в поток, а не в ошибку.
- **Прогон на macOS и Linux.** Непроверенным остаётся: настоящий `which` и пути в `PATH`, права
  `0600`/`0700`, запуск CLI без обёртки `cmd.exe`, связка ключей macOS, чувствительность файловой
  системы к регистру. Два теста, которым нужен настоящий POSIX, честно помечены пропущенными.
- **Автозапуск проекта через настоящий перезапуск панели.** Тумблер, память порта и подъём проектов
  при старте покрыты тестами; сценарий «выключил панель — включил — проект поднялся сам» вживую не
  проходили.
- **Запуск проекта на yarn и на не-Vite стеках.** Живьём проверены pnpm-монорепа (корень и пакеты),
  чтение порта из вывода Vite и сервер, слушающий только `::1`. Выбор yarn, а также Next, Angular и
  create-react-app закрыты тестами по их формату вывода, но настоящего проекта под рукой не было.
- **Три вещи из второй волны багов — просто увидеть в браузере:** ссылка авторизации у MCP-сервера,
  пометка «не отработал» у хука, который не запустился, блок доступа в песочнице.
- **Значок отставания от удалённого (`↓N`) на кнопке ветки.** Сам pull и список изменённых файлов
  проверены в браузере, но репозиторий в момент съёмки был впереди, а не позади, поэтому значок
  вживую не показывался. Закрыт тестом на паре настоящих репозиториев.

## Границы продукта

Невозможное (ограничение Claude Code, ОС или сети) и осознанно ненужное — не задачи, а описание
границ: [LIMITATIONS.ru.md](docs/LIMITATIONS.ru.md).
