/**
 * Отрисовщик файла PowerPoint. Сам код лежит в `pptx/`: сетка слайда и семь
 * раскладок — разные предметы, и в одном файле они перестали помещаться.
 *
 * Этот файл оставлен фасадом, чтобы прежние пути импорта (`deck/pptx.ts`) и
 * тесты продолжали работать: точка входа у колоды одна.
 */
export { renderDeckPptx } from './pptx/index.ts';
