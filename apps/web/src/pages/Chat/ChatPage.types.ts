/**
 * Вложение в том виде, в каком страница чата передаёт его прогону: имя и
 * содержимое в base64. Композер даёт ещё и размер — здесь он не нужен.
 */
export interface ChatSendFile {
  name: string;
  base64: string;
}
