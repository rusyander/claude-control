export interface TestsReleaseCardProps {
  projectPath: string | undefined;
  /**
   * Вехи от свежей к старой — те, что встречаются в истории прогонов.
   *
   * Приходят снаружи, из уже загруженного отчёта: своим запросом карточка
   * спрашивала бы у сервера то, что вкладка только что прочитала, а за
   * требованиями вехи сервер ходит в Jira.
   */
  releases: string[];
}
