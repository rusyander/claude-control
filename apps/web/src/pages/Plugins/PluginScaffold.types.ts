import type { PluginScaffoldComponents } from '@agentdeck/contracts';

/** Ключ одной части каркаса — для перебора переключателей в форме. */
export type ComponentKey = keyof PluginScaffoldComponents;
