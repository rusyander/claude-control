# Сгенерированное — формат задаёт инструмент, который это пишет.
# pnpm перезапишет lock-файл своим форматом при первой же установке,
# отчёт покрытия целиком собирает istanbul.
pnpm-lock.yaml
coverage/
apps/*/coverage/
dist/
.qa-screenshots/
# Телефон: generated Expo files (typed routes, env shim) — их пишет expo, не человек.
apps/mobile/.expo/
apps/mobile/expo-env.d.ts
# Локальный рабочий слой агента — вне git (.git/info/exclude), prettier его не видит через .gitignore.
.agent/
