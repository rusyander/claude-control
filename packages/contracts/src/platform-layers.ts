/**
 * Идентификаторы наших слоёв — общий словарь сервера, экрана панели, телефона и
 * следа запроса.
 *
 * Вынесено из `platform.ts` отдельным модулем ровно по одной причине: телефон
 * берёт этот список КАК ЗНАЧЕНИЕ, а `platform.ts` первой строкой импортирует
 * zod, которого в `node_modules` мобильного приложения нет (у него своя
 * линковка, npm вместо pnpm-воркспейса). Metro резолвит такие модули напрямую в
 * исходник по списку в `apps/mobile/metro.config.js`, и каждый из них обязан
 * быть самодостаточным.
 *
 * Поэтому здесь НЕ ДОЛЖНО появиться ни одного импорта. Нужна связь с другим
 * модулем контрактов — она живёт на стороне того модуля, не здесь.
 */
export const ourLayerIds = ['settings', 'skills', 'mcp', 'systemPrompt'] as const;

export type OurLayerId = (typeof ourLayerIds)[number];
