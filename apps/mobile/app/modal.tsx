import { Redirect } from 'expo-router';

/**
 * Маршрут остался от шаблона Expo. Своего экрана здесь нет — приложение ведёт
 * человека в разговор, а не в пустое модальное окно. Файл сохранён потому, что
 * его удаление не согласовано; поведение при этом честное.
 */
export default function ModalScreen() {
  return <Redirect href="/" />;
}
