import type { Dictionary } from './ru';
import { serverMessagesEn } from './server-messages/en.ts';

/**
 * English mirrors `ru.ts` and is typed against it: a missing or renamed key
 * fails the build instead of showing up as a blank line on the screen.
 */
export const en: Dictionary = {
  serverMessages: serverMessagesEn,
  tabs: {
    chat: 'Chat',
    projects: 'Projects',
    analytics: 'Analytics',
    settings: 'Settings',
    agent: 'Agent',
  },

  common: {
    loading: 'Loading…',
    empty: 'Empty',
    notConnected: 'Panel is not connected. Open Settings.',
    notConnectedChat: 'Panel is not connected. Open Settings and pair the app by QR code.',
    panelSilent: 'The panel did not answer',
    notFound: 'Not found',
    up: 'Up',
    open: 'open',
    close: 'close',
    kb: 'KB',
    duration: { h: 'h', m: 'm', s: 's' },
  },

  chat: {
    title: 'Chat',
    conversations: 'Conversations',
    code: 'Code',
    tests: 'Tests',
    homeChat: 'Home chat',
    blank: 'Empty. Write a task — it goes to the agent on your computer.',
    queued: (prompt: string) => `Queued: ${prompt} ✕`,
    image: '[image]',
    // Предложения панели: карточек на телефоне нет, решают в панели, — но и
    // сырой JSON вместо них показывать незачем.
    offerSplit: 'The agent offered to split the tasks across chats — decide in the panel.',
    offerHandoff: 'The agent offered to continue in a clean session — decide in the panel.',
    picture: {
      title: 'Agent drawing',
      broken: 'The drawing could not be rendered on the phone — open it in the panel.',
    },
    offerDeck: 'The agent dictated a presentation — the card is in the panel.',
    blocksRejected: (count: number) => `Blocks the panel did not accept: ${count} — shown as is.`,
    plan: (done: number, total: number) => `Plan ${done}/${total}`,
    subagents: (count: number) => `Subagents: ${count}`,
    permission: 'Permission needed',
    allow: 'Allow',
    deny: 'Deny',
    permissionFailed: 'Could not answer',
    permissionLost:
      'The decision did not reach the agent: the request is already closed or the run has finished',
    usage: {
      title: 'Step spend',
      input: 'Fresh input',
      cacheCreation: 'Written to cache',
      cacheRead: 'Read from cache',
      output: 'Generated',
      total: 'Total',
      cost: 'Cost',
      answer: 'Answer',
      run: 'Turn spend',
      shared: (count: number) => `Shared spend across ${count} calls of this step`,
      step: 'Step time',
      span: (from: string, to: string) => `from ${from} to ${to}`,
      runTime: 'Whole run',
      live: (time: string) => `running ${time}`,
    },
  },

  composer: {
    ask: 'What should be done?',
    queue: 'Add to the queue…',
    send: 'Send',
    stop: 'Stop',
    settings: 'Sending options',
    attach: 'Attach an image',
    voice: 'Voice',
    voiceListening: 'Listening…',
    voiceDenied: 'No microphone permission',
    voiceUnavailable: 'Speech recognition is unavailable on this device',
    allowEdits: 'Edits allowed',
    autoApprove: 'Auto-approve',
    model: 'Model',
    modelDefault: 'default',
    effort: 'Thinking depth',
    unsupported: (names: string) => `Not supported: ${names}`,
    shot: (index: number) => `shot-${index}.jpg`,
    platformModel: (title: string, model: string) => `Through the contour “${title}”: ${model}.`,
    platformModelReplaced: (title: string, asked: string, model: string) =>
      `Through the contour “${title}”: it has no “${asked}”, the request goes with ${model}.`,
    platformModelUnset: (title: string) =>
      `The contour “${title}” assigned no model (empty catalog or no probe yet) — the request goes as is.`,
    platformNoEffort: (title: string) =>
      `The contour “${title}” does not accept thinking depth — it is not sent.`,
    platformLocked: (title: string) =>
      `This chat goes through the contour “${title}”: it sets the model and depth. Change it in the panel's “Contour” section or return to the default provider.`,
    platformModelNone: 'contour model',
    platformEffortOff: 'not sent',
    platformRefused: (title: string, reason: string, fix: string) =>
      `The contour “${title}” is required, but ${reason}: the message will be refused — it goes neither to the contour nor to the vendor cloud. ${fix}`,
    platformBypassed: (title: string, reason: string, fix: string) =>
      `Bypassing the contour: “${title}” is set to “best effort”, but ${reason} — the message goes straight to the vendor cloud, without the contour’s protection. ${fix}`,
    platformRefusedReason: {
      gateway_down: 'the panel gateway is not up',
      no_token: 'the contour key is not saved',
    },
    platformRefusedFix: {
      gateway_down: 'Press “Start gateway” on the contour card in the panel (“Contour” section).',
      no_token: 'Save the key in the panel: “Configure” on the contour card → “Key” step.',
    },
    platformLayers: (title: string, list: string) =>
      `Through the contour “${title}” the run goes without our: ${list}.`,
    platformLayersAll: (title: string) =>
      `Through the contour “${title}” the run goes without any of our layers: no rules, hooks, permissions, skills, MCP servers or panel prompt addition.`,
    layerTitle: {
      settings: 'Personal rules, hooks and permissions',
      skills: 'Skills',
      mcp: 'MCP servers',
      systemPrompt: 'Panel addition to the system prompt',
    },
    mode: {
      text: 'Message',
      image: 'Image',
      imageBlocked: 'Nothing to draw with',
      imagePlaceholder: 'Describe the image — the panel draws it itself, without the agent',
      imagePlaceholderAgent: 'Describe the image — the agent draws it as code, a vector',
      drawing: 'Drawing — this takes minutes',
      source: (title: string, model: string) => (model ? `${title} · ${model}` : title),
      sourceAgent: 'The chat agent draws it: a vector in code, not a photo',
      promptSkipped: 'The mode prompt is not sent here: the images endpoint has no system message',
      blocked: {
        'no-route': 'No active contour and no endpoint profile — nobody to draw',
        'driver-none': 'This contour does not draw images: its driver declares no such capability',
        'no-model': 'The key catalog has no image-generation model',
        'endpoint-no-url': 'The endpoint profile has no image generation address',
        'gateway-off': 'The panel gateway is off, and contour requests go through it',
        'endpoint-api-kind': 'This API kind has no separate images endpoint',
        'no-agent': 'No raster route and no chat to ask an agent in',
      },
      noRaster: {
        'no-route': 'no raster: no active contour and no endpoint profile',
        'driver-none': 'no raster: this contour does not draw images',
        'no-model': 'no raster: the key catalog has no image-generation model',
        'endpoint-no-url': 'no raster: the endpoint profile has no generation address',
        'gateway-off': 'no raster: the panel gateway is off',
        'endpoint-api-kind': 'no raster: this API kind has no images endpoint',
        'no-agent': 'no raster: nobody to draw',
      },
      card: {
        by: (model: string, source: string) => `Drawn by: ${model} · ${source}`,
        byNoModel: (source: string) => `Drawn by: ${source}`,
        source: {
          'contour-chat': 'contour, as part of the answer',
          'contour-images': 'contour, images endpoint',
          endpoint: 'endpoint profile',
          agent: 'chat agent',
        },
        size: (width: number, height: number, size: string) => `${width}×${height}, ${size}`,
        note: 'A file in the panel, not part of the conversation',
        failed: (message: string) => `Did not work: ${message}`,
      },
    },
  },

  chats: {
    title: 'Conversations',
    newChat: 'New conversation',
    search: 'Search by title',
    nothing: 'Nothing found',
    sandbox: 'sandbox',
    messages: (count: number) => `${count} msg.`,
  },

  projects: {
    toChat: 'To the chat',
    code: 'Code',
    recent: 'Recent',
    browse: 'Browse',
    toDisks: 'to drives',
    openFolder: 'Open this folder',
    chats: (count: number) => `${count} conv.`,
    gone: 'folder is gone',
  },

  git: {
    title: 'Git',
    noCommits: 'no commits yet',
    detached: 'HEAD detached',
    noUpstream: 'no upstream',
    dirty: (count: number) => `changed: ${count}`,
    clean: 'tree is clean',
    noRemote: 'no remote',
    showFiles: 'Show files',
    hideFiles: 'Hide files',
    truncated: '…list truncated',
    staged: 'staged',
    commitMessage: 'Commit message',
    commit: 'Commit',
    pull: 'Pull',
    push: 'Push',
    newBranch: 'New branch',
    create: 'Create',
  },

  code: {
    title: 'Code',
    projectTitle: 'Project code',
    noProject: 'No project selected. Open one on the Projects tab.',
    root: 'Project root',
    changedHere: 'Changed in this conversation',
    missing: 'not on disk',
    unbound: (count: number) => `Edits that could not be matched: ${count}`,
    dirFailed: 'Directory unavailable',
    partial: 'Not the whole directory is shown',
    fileFailed: 'The file did not open',
    readOnly: 'read-only',
    tooBig: 'too big to compare',
    unmatched: (count: number) => `edits not found: ${count}`,
    wholeFile: 'file rewritten whole',
    showFile: 'show the file',
    showDiff: 'show the edits',
    binary: 'A binary file — nothing to show.',
  },

  tests: {
    title: 'Tests',
    screenTitle: 'Project test cases',
    noProject: 'No project selected. Open one on the Projects tab.',
    where: (dir: string) => `Cases live in ${dir} inside the project itself.`,
    empty: 'This project has no tests yet.',
    emptyHint: 'Let the agent look around the app and write the first set.',
    emptyGroup: 'This group has no cases.',
    broken: (error: string) => `The group file did not parse: ${error}`,
    brokenHint: 'The panel leaves it alone — fix the file on the computer.',
    generate: 'Generate',
    run: 'Run',
    runFull: 'Full retest',
    stop: 'Stop',
    scope: 'What to focus on: chat only, for example',
    release: 'Milestone: v1.4 · empty — from a git tag',
    onComputer: 'The run happens on the computer: the agent starts the app where the code lives.',
    links: {
      title: 'Linked to',
      issue: 'Issue',
      page: 'Requirements',
    },
    running: 'Run in progress',
    generating: 'The agent is writing cases',
    done: 'Run finished',
    stopped: 'Run stopped',
    failed: (error: string) => `The run broke: ${error}`,
    log: 'Run log',
    counts: (passed: number, failed: number, rest: number) =>
      `${passed} passed · ${failed} failed · ${rest} left`,
    conventionOff: 'Chat does not keep the cases',
    conventionInstall: 'Write the convention into CLAUDE.md',
    conventionOn: 'Chat keeps the cases too',
    addCase: 'Add a test',
    editCase: 'Edit test',
    caseTitle: 'What we check',
    casePurpose: 'Why',
    caseArea: 'Area',
    caseSection: 'Section',
    caseSectionHint: 'Path inside the group: Chat/Attachments',
    casePrecondition: 'Starting state',
    caseSteps: 'Steps',
    caseExpected: 'Expected result',
    caseOracle: 'How it is proven',
    caseTags: 'Tags, comma separated',
    caseType: 'Type',
    casePriority: 'Priority',
    caseAutomation: 'Automation',
    stepAction: (index: number) => `Step ${index}`,
    stepExpected: 'Step expectation',
    stepAdd: 'Add a step',
    stepRemove: 'Remove',
    save: 'Save',
    remove: 'Delete',
    source: { agent: 'written by the agent', human: 'written by you' },
    status: {
      unknown: 'not run',
      running: 'running',
      passed: 'passed',
      failed: 'failed',
      skipped: 'skipped',
      blocked: 'blocked',
    },
    kind: { case: 'case', checklist: 'checklist' },
    priority: { blocker: 'blocker', high: 'high', medium: 'medium', low: 'low' },
    automation: { manual: 'by hand', toAutomate: 'to automate', automated: 'automated' },
    filter: {
      title: 'Filter',
      any: 'any',
      anyTag: 'any tag',
      reset: 'Reset the filter',
      shown: (shown: number, total: number) => `Showing ${shown} of ${total}`,
      nothing: 'Nothing matches the filter.',
    },
    lastRun: (when: string) => `Last run: ${when}`,
    lastRunNever: 'Never run',
    muted: 'quarantine',
    muteReason: (reason: string) => `Quarantine: ${reason}`,
    selected: (count: number) => `Selected: ${count}`,
    clearSelection: 'Clear the selection',
    runs: {
      open: 'Run history',
      title: 'Run history',
      empty: 'No runs yet.',
      summary: (passed: number, failed: number, skipped: number, blocked: number) =>
        `${passed} passed · ${failed} failed · ${skipped} skipped · ${blocked} blocked`,
      mode: {
        generate: 'generation',
        run: 'agent run',
        explore: 'exploration',
        automate: 'automation',
        manual: 'manual run',
        import: 'CI import',
      },
      actor: { agent: 'agent', human: 'human', ci: 'CI' },
      state: {
        running: 'in progress',
        done: 'finished',
        error: 'broke',
        stopped: 'stopped',
      },
      spent: (tokens: number) => `${tokens} tokens`,
    },
    manual: {
      open: 'Run by hand',
      openSelected: (count: number) => `Run by hand (${count})`,
      title: 'Manual run',
      none: 'No manual run started.',
      noneHint: 'Select cases in the list and press "Run by hand".',
      position: (index: number, total: number) => `${index} of ${total}`,
      steps: 'Steps',
      note: 'What you actually saw',
      passed: 'Passed',
      failed: 'Failed',
      skipped: 'Skip',
      blocked: 'Blocked',
      prev: 'Back',
      next: 'Next',
      finish: 'Finish the run',
      cancel: 'Drop the run',
      finished: 'The run is written to the history.',
      progress: (done: number, total: number) => `Marked ${done} of ${total}`,
      onDevice:
        'You do the checking, the panel only records it. The result lands in the same file ' +
        'as an agent run.',
    },
  },

  analytics: {
    today: 'Today',
    days: (count: number) => `${count} days`,
    all: 'All time',
    from: 'from',
    to: 'to',
    reset: 'Reset',
    mb: 'MB',
    estimate: 'estimated at API rates',
    tokens: 'Tokens',
    requests: 'Requests',
    cached: 'From cache',
    activeSessions: 'Sessions now',
    byDay: 'By day',
    models: 'Models',
    projects: 'Projects',
    tools: 'Tools',
    skills: 'Skills',
    runningNow: 'Running now',
    recentSessions: 'Recent sessions',
    scanned: (files: number, ms: number) => `Files scanned: ${files} in ${ms} ms`,
    running: ' · running',
    tokensShort: 'tok.',
    allProjects: 'All projects',
    allModels: 'All models',
    filters: 'Filters',
    nothingForFilter: 'Nothing matches these filters in this period.',
  },

  settings: {
    panel: 'Panel',
    online: 'online',
    offline: 'not answering',
    notPaired: 'Not connected',
    pair: 'Pair',
    repair: 'Pair again',
    disconnect: 'Disconnect',
    language: 'Language',
    languageRu: 'Русский',
    languageEn: 'English',
    notifications: 'Notifications',
    notificationsAbout:
      'They arrive when work is finished, has failed, waits for a permission or the agent asks a ' +
      'question. Only the kind of event and the project folder name leave the machine — no text, no code.',
    enableHere: 'Enable on this phone',
    panelNotifies: 'The panel sends notifications',
    testNotification: 'Test notification',
    sentTo: (devices: number) => `Sent to devices: ${devices}`,
    devices: 'Devices',
    forget: 'forget',
    outside: 'Access from outside',
    outsideOn: 'On: the panel requires a token on every request.',
    outsideOff: 'Off: only the panel’s own machine can reach it.',
    serveOn: ' (serve is running)',
    serveOff: ' (serve is not running)',
    noTailscale: 'Tailscale was not found on the machine — there is no address from outside.',
    pushOn: 'Notifications are on',
    thisPhone: 'Phone',
  },

  platform: {
    title: 'Contour',
    readOnly: 'Read-only: the key is entered in the panel, on its own machine.',
    empty: 'No contours.',
    state_ok: 'working',
    state_off: 'not active: the work does not go through it',
    state_noKey: 'no key saved',
    state_failed: 'the probe did not pass',
    state_unchecked: 'not checked yet',
    state_exhausted: 'the contour refused on budget',
    budget: (spent: string, budget: string, percent: number) =>
      `Spend estimate: $${spent} of $${budget} (${percent}%)`,
    budgetOff: (spent: string) => `Spend estimate: $${spent}, no budget set`,
    active: (title: string) => `${title} — active`,
    smokeOk: (answer: string, model: string) =>
      `Test request went through: “${answer}”, model ${model}`,
    smokeFailed: (detail: string) =>
      detail ? `The test request failed: ${detail}` : 'The test request failed',
    estimate: "This is the panel's estimate by its own price table, not the contour's bill.",
    exhaustedAt: (at: string, keyBudget: boolean, level: string) => {
      if (keyBudget) return `Refused ${at}: the key budget is exhausted`;
      return level ? `Refused ${at}, limit: ${level}` : `Refused ${at}`;
    },
  },

  pair: {
    title: 'Connecting',
    screenTitle: 'Pairing',
    fromPanel: 'Code from the panel',
    where: 'Settings → Remote access. Point the camera at the code.',
    allowCamera: 'Allow the camera',
    checkingCamera: 'Checking access…',
    orManually: 'Or by hand',
    address: 'https://machine.tailnet.ts.net',
    token: 'Access token',
    connect: 'Connect',
    needAddress: 'The panel address is required',
    notPanelCode: 'This is not the panel’s code',
    saveFailed: 'Could not save',
    secretNote:
      'The token opens the panel’s whole API: it lives in the phone’s secure storage and goes ' +
      'nowhere else. Lost the phone — rotate the token in the panel and this device stops working.',
  },

  push: {
    channel: 'Agent runs',
    denied: 'Notifications are blocked in the system settings',
    noProjectId: 'No EAS projectId — run `eas init` in apps/mobile',
    noToken: 'The Expo service did not issue a token',
  },

  api: {
    notConfigured: 'The panel is not configured: enter the address and the token',
    silent: 'The panel’s server did not answer',
    unreachable: 'The panel’s server is unreachable',
    tokenRejected: 'The token was rejected — pair the app again',
  },

  agent: {
    title: 'Panel agent',
    subtitle:
      'Type or dictate — the agent does it in the panel. Every change waits for your decision.',
    views: { conversation: 'Conversation', history: 'History', journal: 'Trail' },
    empty:
      'For example: “Create project C:/work/demo”. The agent opens panel pages on the computer.',
    placeholder: 'What should be done in the panel?',
    send: 'Send',
    stop: 'Stop',
    newConversation: 'New conversation',
    thinking: 'The agent is thinking…',
    context: (project: string) => `Project: ${project}`,
    toolCalled: (name: string) => `Action: ${name}`,
    toolFailed: (name: string) => `Action failed: ${name}`,
    stopped: 'Turn stopped.',
    runFailed: (message: string) => `The agent did not answer: ${message}`,
    cut: 'The connection dropped before the turn ended — the agent was stopped. What was said is kept in history.',
    refusal: {
      invalid_body: 'Message not accepted: the server did not understand the request.',
      busy: 'The agent is still answering in this conversation — wait for the turn to end.',
      provider_unsupported:
        'The panel agent works only with Claude Code for now: the active CLI is different.',
      cli_not_found:
        'Claude Code was not found in the computer PATH: the agent has nothing to run on.',
      endpoint_unsupported:
        'The assistant uses its own endpoint: its key would have to be handed to the agent process.',
      contour_unreachable:
        'The assistant goes through a contour, but the gateway is down or has no key — no silent fallback to the vendor cloud.',
      data_mask_broken:
        'The data masking rules are broken: without the mask no message goes to the agent.',
    } as Record<string, string>,
    card: {
      heading: 'The agent asks for confirmation',
      dangerHeading: 'Dangerous action — check carefully',
      approve: 'Run',
      reject: 'Reject',
      expires: (time: string) => `Waits until ${time}`,
      sent: 'Decision sent, waiting for the result…',
      truncated:
        'The preview is not complete — it cannot be approved. Reject and ask to split the action.',
      diff: 'What changes',
      truncatedRefused:
        'The server refused the approval: the preview is incomplete. Reject the card.',
      alreadyDecided: 'This card has already been decided.',
      gone: 'The card is gone — it was withdrawn or timed out.',
      failed: (message: string) => `Decision not accepted: ${message}`,
    },
    outcome: {
      done: 'done',
      rejected: 'rejected',
      timeout: 'timed out',
      invalid: 'invalid input',
      unknown: 'no such action',
      failed: 'failed',
      'needs-secret': 'key needed — enter it in the panel on the computer',
      cancelled: 'agent stopped before the decision',
    },
    decidedBy: {
      auto: 'no question',
      human: 'decided by a human',
      timeout: 'by timeout',
      client: 'agent left',
    },
    history: {
      empty: 'No conversations yet',
      messages: (count: number) => `Messages: ${count}`,
      failed: 'Could not load conversations',
    },
    journal: {
      empty: 'No agent actions yet',
      failed: 'Could not load the action trail',
    },
    pendingBadge: (count: number) => `Waiting for decision: ${count}`,
  },

  run: {
    serverUnreachable: 'The server is unreachable',
    connectionLost: 'Lost the connection to the panel',
    reconnecting: 'Connection lost, reconnecting…',
    answered: (status: number) => `The server answered ${status}`,
    finished: 'Work is finished',
    failed: 'The run failed',
    homeChat: 'home chat',
    needToken: 'The panel requires a token: pair the app again.',
    notSent: {
      busy:
        'The previous answer is still being generated — the message was not sent. ' +
        'Showing the running answer: interrupt it with the Stop button.',
      files: (names: string, supported: string) =>
        `The panel cannot pass such attachments: ${names}. The message was not sent. ` +
        `Allowed extensions: ${supported}.`,
      workspaceMissing: (cwd: string) =>
        `The working folder of this conversation was not found: ${cwd}. It can only be continued from there.`,
      other: (message: string) => `The message was not sent: ${message}`,
    },
  },
};
