# Очередь работ

Что осталось сделать в панели. Разделено по тому, чем задача закрывается: **кодом** — можно взять и
сделать прямо сейчас; **живой проверкой** — код написан и покрыт тестами, но никто не видел его на
настоящей машине, и написать тут нечего, пока не посмотришь. Внутри каждой части — по серьёзности:
что происходит с пользователем, если не делать. Завершённое отсюда убирается: его история — в
коммитах.

## Решается кодом

## Партия «универсальный контур» — план написан 11.09.2026, старт по команде

Контур как провайдер по умолчанию для всего приложения: один активный контур с пробным запросом,
маршрут по потребителям (чат/группы/тесты/раннер/ассистент/чужие CLI/терминал) в момент запуска,
прослойка инструментов в шлюзе (агент Claude Code через контур — доказано прогоном), два списка правил
с матрицей конфликтов, модель и усилие, каталог промптов, картинки и презентации в чате, драйвер-манифест
с набором соответствия для следующих платформ. План, факты разведки, результаты прогонов, компромиссы,
задачи Т0–Т13 и развилки для владельца — `TASKS-CONTOUR.md`.

## Партия «агент панели» — заведена 14.09.2026, старт по команде

Глобальный агент в приложении выполняет просьбы словами теми же действиями, что человек руками: создать
проект в чате, написать тест-кейсы, подключить контур по ссылке на стенд. Реестр действий с классами
риска, третий переходник MCP (только адрес панели, ключи агент не видит), подтверждение изменений,
навигация по страницам. Заказ, что есть сегодня, эталонное решение, задачи А0–А9 и развилки —
`TASKS-PANEL-AGENT.md`.

## Справка: что переделано на эталон, что осталось прежним (на 11.09.2026)

Эталон задан разделом «Контур» и повторён ещё дважды. Такой раздел состоит из трёх вещей:
**путеводитель по настоящим кадрам** (снимки живой панели, не рисунки), **схемы механизма**
(кадр показывает состояние, схема — как оно устроено) и **сверка с кодом**: в
`apps/web/public/help/sources.json` записано, какие модули описывает документ и какими они были в
момент последней вычитки. Код ушёл вперёд — `pnpm shots` краснеет и называет раздел, файлы и команду
`node tools/help-shots/sources.mjs <раздел>`. Сейчас страж следит за **3 разделами из 25**.

Переделаны (три раздела):

| Раздел                      | Кадров                          | Схемы                                         | Что появилось нового                                                                                                                  |
| --------------------------- | ------------------------------- | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| **Контур** (`/platform`)    | 18, один сценарий `connect`     | 3: путь запроса, две системы, кто что заводит | Путеводитель от входа в админку контура до карточки в панели; таблица кодов отказа; реестр подписанных компромиссов                   |
| **Чат** (`/chat`)           | 17: `basics` 10 + `split` 7     | 2: путь одного сообщения, конвейер разделения | Два сценария по входу: один разговор целиком и один разговор, ставший многими; разбор документа на `Chat{Guide,Field,Limits}Sections` |
| **Тестирование** (`/tests`) | 18: `workspace` 10 + `health` 8 | 2: где живут кейсы, что краснит вердикт вехи  | Первый набор от пустого раздела до отчёта и «набор, который уже живёт»; разбор на `Tests{Guide,Health,Exchange,Limits}Sections`       |

Кадры лежат в `apps/web/public/help/<раздел>/<сценарий>/`, схемы — в `…/diagrams/`, исходники схем —
в `docs/diagrams/<раздел>-guide/` (генератор + `.drawio`, не картинка руками). Съёмка: `tools/help-shots/`.

Остались в прежнем виде — **22 раздела**: проза и таблицы, без кадров, без схем, не под стражем
свежести. Это `overview`, `search`, `analytics`, `rules`, `claudeMd`, `skills`, `commands`, `hooks`,
`scripts`, `plugins`, `mcp`, `permissions`, `env`, `integrations`, `projects`, `groups`, `history`,
`compare`, `settings`, `endpoints`, `dlp`, `providers`. Они не сломаны и не устарели по содержанию —
просто написаны словами.

**Партия «вся справка на эталон» — план написан 11.09.2026, старт по команде.** Все двадцать два
раздела доводятся до вида «Контура»: путеводитель по настоящим кадрам, схемы механизма, снимок рядом
с каждым описанным поведением, оба языка, страж свежести. Восемь пачек, три исполнителя, враждебный
ревьюер на каждую пачку. Требования, разбивка, порядок и правила параллельной работы —
[TASKS-HELP.md](TASKS-HELP.md).

Партии от 04.09.2026 «аудит чата» (Т1–Т12), от 06.09.2026 «паритет телефона и качество» (Т1–Т6) и от
06.09.2026 «эталон» (Т1–Т5) закрыты 06.09.2026; разбор каждой задачи — в
`.agent/archive/TASKS-done-2026-09-06.md` (локальный файл вне git).

## Партия от 07.09.2026 «подбор модели под задачу» — сдана

Классификация работы агентом и назначение модели панелью (таблица `contracts/model-cascade.ts`),
конвейер «работа → ревью → фикс» на копиях веток, сводка звеньев у родителя, подбор модели и ревью у
чужих провайдеров, журнал понижённых прогонов с расходом по классам. Приёмка этапов 1 и 2 пройдена
живьём 07.09 без единой подмены API; последний пункт тумана — расход по классам для человека —
закрыт 08.09. Что именно сделано и чем проверено — `.agent/archive/TASKS-done-2026-09-08.md`.

## Партия от 07.09.2026 «внешние интеграции + список нехватки Test IT» — сдана

Atlassian (облако и Server/DC), фордж по токену, Telegram, Zephyr/Xray, junit из CI, привязка
проекта и групп к задачам и страницам, собственный MCP-переходник для агента; шесть дыр из отчёта по
тестовому контуру — матрица покрытия, статус дефекта из трекера, устойчивый `externalId`, карантин,
вехи и релизы, общий вебхук; переходник MCP у остальных девяти CLI. Что именно сделано и чем
проверено — `.agent/archive/TASKS-done-2026-09-07.md`.

Открытым остаётся только то, что нельзя проверить без ваших ключей, — см. следующий раздел.

## Партия от 08.09.2026 «руководство по тестам и управляемая генерация» — сдана

Генерация пишет черновик, а файлы кейсов пишет только панель; источники генерации (требование,
дифф, дефект, упавший кейс); линтер и дубликаты; планы правилом; кнопки `explore` и `automate`;
сравнение прогонов и перепрогон провалов; доказательство провала; карантин с самоочисткой; риск и
время; отчёт готовности релиза; секреты окружения; эргономика ручного прохода; паритет CLI и CI;
окружения, общие шаги и свои поля с экрана; руководство `docs/TESTS-GUIDE.ru.pdf`. Что именно
сделано и чем проверено — `.agent/archive/TASKS-done-2026-09-08.md`.

### Ещё не специфицировано (туман, не тикеты)

- **Проверка самого набора поломкой.** Единственный честный ответ на «а поймают ли эти кейсы
  регрессию» — внести заведомую поломку и убедиться, что нужный кейс покраснел. Механика в панели уже
  есть: параллельная копия репозитория (`worktrees`), права прогона, автотесты у части кейсов. Дорого
  и по времени, и по окну лимитов, поэтому берётся только по отдельному вашему слову.
- **Кейсы из живого разговора.** Транскрипт чата содержит сценарий, который человек только что
  прошёл руками; превращать его в кейс — заманчиво, но чат ведёт соседняя сессия, и трогать его
  сейчас нельзя.
- **Свои типы проверок** (доступность, скорость) отдельным видом кейса — пока не ясно, чем такой
  кейс отличается от обычного, кроме оракула.
- **Junction вместо копии для `.claude/skills` и `.claude/hooks`** — живое обновление скиллов в копиях;
  пока копирование плюс кнопка «Обновить».
- **Защита dev-сервера от перезапуска при живых прогонах** — `node --watch` рестартует на любой
  правке; усыновление прогонов (сдано 09.09) убрало потерю ответов, но правка сервера всё ещё обрывает
  живой ход — либо отложенный рестарт, либо явная пауза на время прогонов.
- **Слияние веток разделения из панели** — сегодня осознанно вне продукта. Сверка пересечений сдана
  09.09, то есть повод пересмотреть появился; решение за владельцем.

## Партия от 09.09.2026 «автономный конвейер» — сдана 09.09.2026

Десять задач кодом плюс замер объёма для чужих CLI (Т11): план сильной модели перед работой слабой,
зеркало локального слоя и бутстрап копии, старт агента в копии без ручных шагов, перезапуск сессии,
усыновление прогонов после перезапуска панели, сверка веток, ревью по ссылкам на MR, время работы в
бейдже, пауза и продолжение всего дерева. Сдано коммитом `e6d4498`; спецификации, решения и чем
проверено — [.agent/archive/TASKS-done-2026-09-09.md](.agent/archive/TASKS-done-2026-09-09.md).

## Партия от 09.09.2026 «автономия у чужих CLI» — сдана 10.09.2026

Семь задач (Т1–Т7) по итогам замера Т11: у чужих CLI появились время работы в ленте, связи и дерево
чатов, уровни разбора и плана, сверка веток, пауза дерева, ревью чужих MR по ссылке и продолжение в
чистой сессии. Спецификации, решения и чем проверено —
[.agent/archive/TASKS-done-2026-09-10.md](.agent/archive/TASKS-done-2026-09-10.md).

## Партия от 09.09.2026 «контур EnterprisePlatform» — план в отдельных файлах

Слишком велика для этой очереди: четырнадцать задач, сквозной механизм подписи компромиссов и
собственный раздел интерфейса. Здесь она не дублируется, чтобы не разъезжалась:

- [TASKS-ENTERPRISE_PLATFORM.md](TASKS-ENTERPRISE_PLATFORM.md) — план: решения, инварианты, дизайн, задачи, порядок работ;
- [TASKS-ENTERPRISE_PLATFORM-REFERENCE.md](TASKS-ENTERPRISE_PLATFORM-REFERENCE.md) — что платформа компании даёт снаружи и в каком виде;
- [TASKS-ENTERPRISE_PLATFORM-SETUP.md](TASKS-ENTERPRISE_PLATFORM-SETUP.md) — что нажать в её админке, что заполнить у нас,
  что написать в справке панели.

Перед первой строчкой кода — §12 плана «Параллельность»: что можно вести одновременно, сколько
исполнителей это оправдывает и какие два разрешения нужны на старте.

Партия начата 09.09.2026: **Т0 «Механизм подписи компромиссов» — готово** (реестр четырнадцати
подписей, значок с подсказкой, раздел «Контур» в том объёме, который уже правда, и проверка, роняющая
сборку на немом обходе). Дальше по порядку — Т1. Состояние партии и что где лежит: «Решение» в §7
плана.

## Не решается кодом — нужна живая машина

Здесь нечего писать, пока не запустишь: код есть, тесты зелёные, но подмена — не запуск.

- **Живая приёмка генерации, исследования и доказательства провала (Т2, Т6, Т8 партии 08.09).**
  Линтер, дубликаты, планы правилом, таксономия, карантин и риск приняты живьём 08.09 на настоящей
  библиотеке с историей прогонов — снимки и разбор в `.agent/screenshots/accept-t4-t5-t9-t10/`.
  Осталось то, что нельзя проверить без настоящего запуска агента: генерация в черновик и его
  приёмка, кнопки «исследовать» и «автоматизировать», прогон против намеренно сломанного экрана.
  Это расход окна лимитов, поэтому берётся по вашему слову.
- **Телефон после партии 06.09.** Холодный старт после конца прогона, сторож потока (30 с тишины →
  переподключение), очередь и переподхват после перезапуска закрыты 12 юнит-тестами стора, но на
  устройстве не запускались: собрать APK, повторить сценарии Т1–Т2 из
  `.agent/archive/TASKS-done-2026-09-06.md`.
- **Живая приёмка интеграций — нужны ваши ключи.** Код собран и покрыт тестами с подменой, но
  настоящего Atlassian, настоящего Telegram и настоящего приёмника вебхука панель на этой машине не
  видела. Ключи вводите вы сами в панели (агенту они не передаются): сайт и API-токен Atlassian →
  проверка связи, одна настоящая задача Jira по провалу, одна публикация в Confluence, одно
  сообщение в Telegram, один POST на свой адрес с проверкой подписи, один вызов MCP из настоящего
  прогона агента.
- **Красный статус CI на GitHub.** `.github/workflows/ci.yml` написан, его шаги локально зелёные; что
  намеренно сломанный PR красится в красный, видно только после пуша.
  06.09 прогон не стартовал дважды: «account is locked due to a billing issue» — снять блокировку в
  GitHub, затем `gh run rerun 34027816828` или любой пуш.
- **Сторож `keepalive` не установлен.** Перезапуск dev-сервера 05.09 22:30:54 закрыт как
  невоспроизводимый (`node --watch` перезапускается от одного касания mtime файла из графа импортов;
  правок агентов в ту минуту не было). Следующий такой случай оставит след только после
  `pnpm keepalive:install` — решение владельца.
- **Первая реальная запись у чужих CLI.** Aider, Cursor, OpenCode, Continue, Goose, Kimi Code и Qwen
  Code на машине разработки не установлены: их форматы собраны по документации и закрыты
  round-trip-тестами, но глазами первую запись никто не видел. Проверять по одному: поставить CLI,
  открыть раздел, записать, сверить файл вручную.
- **Первый живой разговор у чужого CLI.** Чат собран на задокументированном неинтерактивном запуске
  и покрыт тестами на форме argv и на потоке, но настоящего ответа настоящего CLI никто не видел.
  Проверять по одному: поставить CLI, задать вопрос, убедиться, что текст идёт по мере печати, а
  второй вопрос помнит первый. У OpenCode отдельно — что сессия `opencode serve` поднимается, а её
  заминка роняет разговор в поток, а не в ошибку.
- **Прогон на macOS и Linux.** Непроверенным остаётся: настоящий `which` и пути в `PATH`, права
  `0600`/`0700`, запуск CLI без обёртки `cmd.exe`, связка ключей macOS, чувствительность файловой
  системы к регистру. Два теста, которым нужен настоящий POSIX, честно помечены пропущенными.
- **Автозапуск проекта через настоящий перезапуск панели.** Тумблер, память порта и подъём проектов
  при старте покрыты тестами; сценарий «выключил панель — включил — проект поднялся сам» вживую не
  проходили.
- **Запуск проекта на yarn и на не-Vite стеках.** Живьём проверены pnpm-монорепа (корень и пакеты),
  чтение порта из вывода Vite и сервер, слушающий только `::1`. Выбор yarn, а также Next, Angular и
  create-react-app закрыты тестами по их формату вывода, но настоящего проекта под рукой не было.
- **Три вещи из второй волны багов — просто увидеть в браузере:** ссылка авторизации у MCP-сервера,
  пометка «не отработал» у хука, который не запустился, блок доступа в песочнице.
- **Значок отставания от удалённого (`↓N`) на кнопке ветки.** Сам pull и список изменённых файлов
  проверены в браузере, но репозиторий в момент съёмки был впереди, а не позади, поэтому значок
  вживую не показывался. Закрыт тестом на паре настоящих репозиториев.

## Границы продукта

Невозможное (ограничение Claude Code, ОС или сети) и осознанно ненужное — не задачи, а описание
границ: [LIMITATIONS.ru.md](docs/LIMITATIONS.ru.md).
